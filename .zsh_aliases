alias art="php artisan"
# alias _="docker-compose run --rm"
alias tinker="art tinker"
alias migrate="art migrate"
alias routes="art route:list"
alias fresh="art migrate:fresh"
alias phpunit="./vendor/bin/phpunit"
alias pat="php artisan test"
alias tfs="php artisan test && php artisan migrate:fresh --seed"
alias sf="php bin/console"
# alias docker_clean="docker stop $(docker ps -aq) && docker rm $(docker ps -aq) && docker rmi $(docker images -aq)"

alias wstorm="open -na \"WebStorm.app\""

alias pip="python3 -m pip"

alias connect_server="ssh -i ~/.ssh/id_akmyradov root@167.99.157.85"

alias nah="git reset --hard;git clean -df;"
alias gl="git log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit"
alias ga="git add"
alias gaa="git add ."
alias gc="git commit -m"
alias gp="git push"
alias gs="git status"

alias sail='[ -f sail ] && sh sail || sh vendor/bin/sail'

# file manipulations

# servers
alias sshcodefrenzies="ssh root@159.223.135.143"
alias sshakmyradov="ssh root@167.99.157.85"

# vim
alias vim=nvim
alias vi=nvim

# helpful
alias getip="ifconfig | grep \"inet\""
